export interface FormState {
    name: string;
    email: string;
    message: string;
}

export interface SocialMediaLink {
    name: string;
    url: string;
    icon: JSX.Element;
    gradientClasses: string;
} 